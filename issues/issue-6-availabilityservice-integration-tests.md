## What to build

Pruebas de integración (InMemory DB) para `AvailabilityService` que validen la consulta de reservas, la integración con `SlotCalculator` y el mapeo a DTO.

## Acceptance criteria

- [ ] Tests que usen datos seed (o setup) y verifiquen slots entregados por el servicio.
- [ ] Casos: sin reservas, con reservas, adyacencias exactas.

## Blocked by

- Blocked by #23
- Blocked by #22
