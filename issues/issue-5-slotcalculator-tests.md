## What to build

Agregar tests unitarios puros para `SlotCalculator` que verifiquen cálculo de huecos y segmentación: adyacencias exactas, solapamientos parciales/total y filtrado por `requiredDurationMinutes`.

## Acceptance criteria

- [ ] Tests para adyacencia (existing.EndUtc == requested.StartUtc) pasen.
- [ ] Tests para solapamiento parcial y total pasen.
- [ ] Tests para filtrado por `requiredDurationMinutes` pasen.

## Blocked by

- Blocked by #23
