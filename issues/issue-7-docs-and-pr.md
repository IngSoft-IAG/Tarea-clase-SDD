## What to build

Actualizar `README.md`/`spec.md` con la nueva ruta de disponibilidad, ejemplos de uso y pasos para probar localmente. Preparar PR que agrupe los cambios (código, tests y documentación).

## Acceptance criteria

- [ ] `README.md` muestra la ruta `GET /api/rooms/{roomId}/availability` con ejemplo de query y respuesta.
- [ ] PR listo que incluya referencias a los issues de implementación.

## Blocked by

- Blocked by #22
- Blocked by #24
- Blocked by #26
